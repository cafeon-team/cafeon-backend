@extends('admin.layout')
@section('title','시스템') @section('heading','시스템 상태 · 감사 로그')
@section('content')
<div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach(['Database'=>'database','Cache'=>'cache','Storage'=>'storage','Kakao Map'=>'kakaoMap','Google Login'=>'googleLogin','Kakao Login'=>'kakaoLogin','Naver Login'=>'naverLogin'] as $label=>$key)<article class="rounded-2xl bg-white p-5 shadow-sm"><div class="flex items-center justify-between"><strong>{{ $label }}</strong><span class="h-3 w-3 rounded-full {{ $checks[$key]?'bg-emerald-500':'bg-red-400' }}"></span></div><p class="mt-3 text-sm {{ $checks[$key]?'text-emerald-700':'text-red-600' }}">{{ $checks[$key]?'정상 / 설정됨':'확인 필요' }}</p></article>@endforeach</div>
<section class="mt-6 overflow-hidden rounded-2xl bg-white shadow-sm"><div class="border-b p-5"><h2 class="text-lg font-black">관리자 작업 이력</h2><p class="mt-1 text-sm text-gray-500">로그인과 중요 데이터 변경 작업을 추적합니다.</p></div><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-[#f5f6f2] text-xs"><tr><th class="p-4">일시</th><th class="p-4">관리자</th><th class="p-4">작업</th><th class="p-4">대상</th><th class="p-4">IP</th></tr></thead><tbody class="divide-y">@forelse($audits as $audit)<tr><td class="p-4 text-gray-500">{{ $audit->created_at?->format('Y-m-d H:i:s') }}</td><td class="p-4 font-semibold">{{ $audit->admin?->email ?? '시스템' }}</td><td class="p-4"><code class="rounded bg-[#edf2d8] px-2 py-1 text-xs">{{ $audit->action }}</code></td><td class="p-4">{{ $audit->target_type }} @if($audit->target_id)#{{ $audit->target_id }}@endif</td><td class="p-4 text-gray-500">{{ $audit->ip_address }}</td></tr>@empty<tr><td colspan="5" class="p-12 text-center text-gray-500">감사 로그가 없습니다.</td></tr>@endforelse</tbody></table></div></section><div class="mt-5">{{ $audits->links() }}</div>
@endsection
